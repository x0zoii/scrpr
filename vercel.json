{
  "builds": [
    {
      "src": "api/app.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.11"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/app.py"
    }
  ],
  "installCommand": "pip install -r requirements.txt --target /tmp/pip-packages && cp -R /tmp/pip-packages/* . && playwright install chromium"
}

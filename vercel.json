{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "maxDuration": 60,
        "memory": 1024
      }
    }
  ],
  "routes": [
    { 
      "src": "/", 
      "dest": "/api" 
    },
    { 
      "src": "/api", 
      "dest": "/api" 
    }
  ],
  "installCommand": "pip install -r requirements.txt && python -c \"import asyncio; import pyppeteer_chromium_resolver as pcr; asyncio.run(pcr.install(chromium_args=['--no-sandbox', '--disable-gpu']))\""
}
